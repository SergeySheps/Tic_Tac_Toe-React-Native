import { StyleSheet } from 'react-native';
// import constants from '../../../constants';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row'
  },
  headerStyle: {
    color: 'black',
    fontSize: 30,
    fontFamily: 'AvenirNext-DemiBold',
    fontStyle: 'italic',
    fontWeight: 'bold'
    // width: constants.winSize.width - 40
  }
});

export default styles;
